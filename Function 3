1) Мы создаем наше собственное приложение для социальной сети, и нам нужна программа для генерации хэштегов.
Завершите программу для вывода входного текста, начинающегося с хэштега (#).
Кроме того, если пользователь ввел несколько слов, программа должна удалить пробелы между ними.
Вы можете использовать функцию replace() для замены пробелов (" ") пустыми строками ("").
Обратите внимание, что функция replace() возвращает новую строку, поэтому вам нужно объявить для нее новую переменную.

s = input()

def hashtagGen(text):
	#your code goes here
	
	return

print(hashtagGen(s))


2)  Когда программа запущена, она должна бросить кубик и вывести результат каждого кубика. 
Используйте функцию random.randint() для генерации случайных значений в диапазоне от 1 до 6 для каждой кости.

import random
random.seed(int(input())) #please don't touch this lane

#generate the random values for every dice
dice1 = 
dice2 = 

print(dice1)
print(dice2)

3) Напишите программу, которая проверяет, кипит ли вода.
Возьмите целочисленную температуру в градусах Цельсия в качестве входных данных и выведите "Кипение", если температура выше или равна 100.

temp = int(input())
# your code goes here

4) Напишите программу для контроля входа в клуб.
В клуб допускаются только лица, достигшие 18 лет и старше.
Данная программа принимает возраст и имя человека, который пытается войти. Завершите программу, 
чтобы вывести "Добро пожаловать", за которым следует имя человека, если ему разрешено входить в клуб,
и "Извините", если он моложе разрешенного возраста.

age = int(input())
name = input()
# your code goes here

5) Комфортная относительная влажность воздуха для человека составляет от 40% до 60%.
Данная программа принимает процент влажности в качестве входных данных.

Задача
Введите код для вывода "норма", если заданный процент влажности находится в диапазоне от 40 до 60 включительно.
В противном случае ничего не выводите.

humidity = int(input())
#your code goes here

